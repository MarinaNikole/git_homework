1) git checkout [file-name]

2) git restore --staged [file name]  

3) git reset HEAD ~ [number]

4) git checkout -b [branch name]

5) В чем отличение между fast-forward merge and non fast-forfard merge?
Fast-forward merge – быстрое перемещение. Происходит, когда мы хотим слить ветку на которой были произведены изменения с веткой без изменений.
Non fast-forward происходит тогда, когда мы собираемся сливать две ветки, и на обоих происходили изменения (на каждой ветке разные изменения)

6) Когда может возникнуть конфликт слияния веток, как его разрешить?
Когда происходит конфликт слияния веток, гит предлагает нам открыть файл, в котором на разных ветках были произведены разные изменения. В этом файле будут содержаться все изменения из двух веток. Гит предложит нам выбрать, какие изменения мы хотим оставить, а какие удалить, или же ничего не удалять. Для этого мы вручную удаляем ненужные символы(сгенерированные гитом), и на наше усмотрение, при необходимости, удаляем информацию, которую посчитаем лишней. Корректируем файл и выходим.
Вводим команду   git status
Затем            git merge – abort    (если хотим отменить слияние)
                 git commit -a    или    git add   (чтобы закончить слияние)
                               
7) git branch -d [branch name]

